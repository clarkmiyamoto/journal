#meeting 

Thoughts after meeting

Consider a state space model, e.g.
$$
\begin{align}
f_t= A f_{t-1}  + \epsilon
\end{align}
$$
where $\epsilon \sim \mathcal N(0, 1)$

I think Justin's question is can we construct a generative model which learns a 