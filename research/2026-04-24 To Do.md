Good morning future Clark, here's what Clark from last night was thinking about

1. Make a separate training file called `train_unsupervised.py` this will run the SCSI algorithm. This will require making a new `training/trainer_unsupervised.py` file
2. Do the same thing for the supervised case. 

Try to get some things working before Friday morning.