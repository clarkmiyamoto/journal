#talk #stochasticprocess #sgd
Presenter: Sibylle Marcotte

# Notes
**Example**
Consider a linear network, $\theta = (u,v)$ where the network is $g(\theta, x) = uv x$.
The empirical risk is $\mathcal L(u,v) = (uvx - y)^2$

In continuous dynamics, you have a conservation along hyperbolic trajectories (think space-time interval in Minkowski metric is invariant)

