#coding #obsidian

There's a nice plugin https://github.com/twibiral/obsidian-execute-code which allows you to write executable code in Obsidian.

At the top of your code block, you add `run-<LANGUAGE>`, so for Python write `run-python`.

Example:
```run-python
print('Hello world')
```
Woah!