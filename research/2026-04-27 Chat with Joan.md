Retry convergence rate gaussian example but in lifted interpolant
$$
\begin{align}
I_t | \mathcal F(X) = \alpha_t Z + \beta_t X
\end{align}
$$

- Is it better to bake in the invariances / equivariances into the architecture, or is it better to let the algorithm find it itself?
- It would be better to have stronger convergence guarentees. E.g. when will this algorithm converge?

Is there a difference between these two lifted options?
![[2026-04-27 options.png]]

Can we get the MRA to work?

___
We'll chat on Friday on how to score the project the direction, so I can just focus on one thing.